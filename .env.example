# .env.example - Variables de entorno del proyecto Porfolio

# Clave secreta para tokens CSRF (genera una aleatoria en producción)
CSRF_SECRET=pon_aqui_una_clave_segura_y_unica

# Entorno de ejecución (development | production)
NODE_ENV=development

# Puerto local del servidor Express
PORT=3000

# (Opcional) Límites de rate limiting por entorno
LIMIT_GLOBAL=100
LIMIT_CONTACTO=5
LIMIT_LOGIN=5

# (Opcional) Configuración de email si se usa SMTP
# EMAIL_SMTP_HOST=smtp.tu-dominio.com
# EMAIL_SMTP_PORT=587
# EMAIL_SMTP_USER=usuario@dominio.com
# EMAIL_SMTP_PASS=contraseña_super_segura
